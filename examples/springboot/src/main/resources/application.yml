server:
  port: 8080
spring:
  main:
    lazy-initialization: true
  security:
    oauth2:
      client:
        registration:
          podspaces:
            client-id: #client-id document URI
            provider: podspaces
            scope:
              - openid
              - webid
            client-authentication-method: none
            authorization-grant-type: authorization_code
        provider:
          podspaces:
            issuer-uri: https://login.inrupt.com