<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.inrupt</groupId>
    <artifactId>inrupt-parent</artifactId>
    <version>7</version>
  </parent>

  <artifactId>inrupt-template</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>Inrupt - TEMPLATE</name>
  <packaging>pom</packaging>

  <properties>
    <!-- dependencies -->
    <quarkus.version>2.10.0.Final</quarkus.version>

    <!-- test dependencies -->
    <wiremock.version>2.27.2</wiremock.version>
  </properties>

  <repositories>
    <repository>
      <releases>
        <updatePolicy>never</updatePolicy>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <id>central</id>
      <name>Maven Central</name>
      <layout>default</layout>
      <url>https://repo1.maven.org/maven2</url>
    </repository>
    <repository>
      <releases>
        <updatePolicy>never</updatePolicy>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
      <id>inrupt-internal</id>
      <name>Inrupt Internal Release Repository</name>
      <url>https://download.software.inrupt.com/basic/internal/maven/</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-bom</artifactId>
        <version>${quarkus.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.github.tomakehurst</groupId>
        <artifactId>wiremock</artifactId>
        <version>${wiremock.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <modules>
  </modules>

  <distributionManagement>
    <snapshotRepository>
      <name>Inrupt Shapshot Repository</name>
      <id>inrupt</id>
      <url>https://maven.cloudsmith.io/inrupt/development/</url>
    </snapshotRepository>
    <repository>
      <name>Inrupt Staging Repository</name>
      <id>inrupt-staging</id>
      <url>https://maven.cloudsmith.io/inrupt/staging/</url>
    </repository>
  </distributionManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-maven-plugin</artifactId>
          <version>${quarkus.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>generate-code</goal>
                <goal>generate-code-tests</goal>
                <goal>build</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-bootstrap-maven-plugin</artifactId>
          <version>${quarkus.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-site-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <!-- TODO: Replace PROJECT -->
  <scm>
    <connection>scm:git:https://github.com/inrupt/enterprise-PROJECT.git</connection>
    <developerConnection>scm:git:https://github.com/inrupt/enterprise-PROJECT.git</developerConnection>
    <url>https://github.com/inrupt/enterprise-PROJECT</url>
    <tag>HEAD</tag>
  </scm>

</project>
